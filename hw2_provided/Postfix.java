public class Postfix {

    // pop operands from the stack, apply op and push the result
    public void pushOperator(String op)
    {
        
    }

    // push a number onto the stack
    public void pushNumber(double d)
    {
        
    }

    // return the top of the stack
    public double value()
    {
        return 0.0;
    }

    // reset the stack
    public void clear()
    {
        
    }
    
}
